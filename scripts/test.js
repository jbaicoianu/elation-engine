/*
var engine = elation.engine("foo", elation.html.create());
engine.systems.add("physics");
engine.systems.add("world");
var controls = engine.systems.add("controls", { 
  "default": {
    "menu": ["keyboard_esc", function() { alert('menu'); }]
  }
});
controls.activateContext("default");

var renderer = engine.systems.add("renderer");
var view = renderer.view_init("main", {fullwindow: true, types: ["webgl", "css"]});

engine.start();
*/
